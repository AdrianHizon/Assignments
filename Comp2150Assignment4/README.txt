1. Place all files into the same directory
2. Enter the directory containing all the files
3. If on Windows, copy and paste the following into the command line: Node.exe Main.js
   If on Linux/Mac, copy and paste the following into the command line: nodejs Main.js